<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Diagnostic Support Request</title>
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.5; color: #333; }
    .container { max-width: 600px; margin: 0 auto; background: #fff; border: 1px solid #eee; border-radius: 8px; overflow: hidden; }
    .header { background: #6c63ff; color: white; padding: 20px; text-align: center; }
    .content { padding: 20px; }
    .footer { padding: 15px; text-align: center; background: #f9f9f9; font-size: 12px; color: #666; }
    .field { margin-bottom: 12px; }
    .label { font-weight: bold; color: #444; }
    img { max-width: 100%; border-radius: 5px; margin-top: 10px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>New Diagnostic Support Request</h1>
    </div>
    <div class="content">
      <p><span class="label">From:</span> {{email}}</p>
      <p><span class="label">Subject:</span> {{subject}}</p>
      <p><span class="label">Message:</span></p>
      <p>{{content}}</p>

      {{#if imageUrls}}
        <p><span class="label">Attached Images:</span></p>
        {{#each imageUrls}}
          <img src="{{this}}" alt="Diagnostic Image" />
        {{/each}}
      {{/if}}

      {{#if pdfUrls}}
  <p><span class="label">Attached PDFs:</span></p>
  <ul>
    {{#each pdfUrls}}
      <li><a href="{{this}}" target="_blank">View PDF</a></li>
    {{/each}}
  </ul>
{{/if}}
    </div>
    <div class="footer">
      <p>&copy; 2025 Diagnostica Support</p>
    </div>
  </div>
</body>
</html>
